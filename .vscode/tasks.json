{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run pytest with HTML report",
            "type": "shell",
            "command": "pytest --cov . --cov-report=html",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        },
        {
            "label": "Run pytest with XML report",
            "type": "shell",
            "command": "pytest --cov . -- cov-report=xml",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}